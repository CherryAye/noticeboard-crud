Notice Board App (CRUD)  

Description
-----------
To add, delete, and update and review list of noteboard records. 
This App was made in Java 17 and the Spring MVC Web framework with SQLite in-memory database. 
Dependencies are requied to download from Maven Repository. List of Dependencies can be found in pom.xml.

Project Build and Run
---------------------
After downloading the source, import the project, compile and run. 
It can either run from maven profile or SpringBootApplication like "Run as Java Application" -> "BoardingManagementWebApplication.java" file.

When a web server is up, access the app through localhost:8080 by using username -> sa and password -> sa. 
Login username and password can alter on application.properties file.
